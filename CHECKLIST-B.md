# 手機版交付檢查清單

交付後由你本人用手機實機掃過，非自動化驗收項目（EXPENSIVE-PLAN-B.md §6.3、§6.4）。
前提：已依 `DEPLOY.md` 建立 public 倉庫並啟用 GitHub Pages。

## 開啟與載入

- [ ] 用手機瀏覽器開 `https://<帳號>.github.io/<倉庫名>/`，頁面標題與副標正確顯示
- [ ] 「正在準備執行環境…」會依序換成下載、載入解析套件、解開處理程式，最後出現選檔區
- [ ] 記下第一次載入耗時：＿＿＿ 秒（Wi-Fi）／＿＿＿ 秒（行動網路）
- [ ] 第二次開啟（非飛航模式）明顯比第一次快

## 選檔與處理

- [ ] 選 5 份 MHT（基本成績、球種情報、狀況別、對戰成績、球團別配球），清單列出檔名與大小
- [ ] 沒選檔時「開始處理」是灰的，選了才可按
- [ ] 按下後 log 第一行是「名冊快照抓取日期 …，共 12 隊」，日期看得出名冊新舊
- [ ] 八階段進度全部變成完成；若有失敗，該列後面會標「（失敗）」
- [ ] 注意：管線在主執行緒同步執行，處理期間畫面可能停住幾秒，跑完才一次顯示全部 log 與進度——這是已知限制，不是當機
- [ ] 只選 1–4 份也能跑完，缺的分頁在 log 裡是 warn，不會整個失敗
- [ ] 選一個不是 MHT 的檔案，log 顯示「無法分類，略過」

## 產出

- [ ] 「開啟報告」在新分頁打開對照表，內容與電腦版本機窗口的報表版面一致
- [ ] 「下載報告」存下的檔名是 `report-<YYYYMMDD-HHMM>.html`，存到手機後可以再打開
- [ ] 報表的交叉驗證區：手機版用 12 隊真實名冊，對左可能顯示 54 打數 9 安打「不符」，
      與本機窗口的 52 打數 8 安打不同——原因見 `out/preview-web.md`，請判斷要以哪個為準
- [ ] 報表頁尾的名冊未命中清單與稽核差異合理

## 離線與主畫面

- [ ] 成功跑過一次後，開飛航模式、關掉分頁再重開網址，頁面仍可開啟並完成載入
- [ ] 飛航模式下再跑一次處理，能產出報表（若 Pyodide 沒被瀏覽器快取住會載入失敗，請記下是哪個瀏覽器）
- [ ] 「加到主畫面」後從主畫面圖示開啟，以獨立視窗顯示（manifest 的 icons 目前是空的，圖示會是瀏覽器預設樣式）

## 版面

- [ ] 小尺寸手機（約 360px 寬）文字不截斷、按鈕好按
- [ ] 平板或橫向時內容置中、最大寬度合理
- [ ] 錯誤區（例如關掉網路後第一次開啟）紅框訊息看得懂

## 更新網站時

- [ ] 改了 `core/`、`adapters/`、`web/pipeline.py`、`config/` 或 `cache/` 後，有跑 `python tools/build_web.py` 再 commit
      （忘了的話 `./verify.sh` 的 `test_published_payload_is_current` 會失敗提醒）
- [ ] 同時把 `docs/sw.js` 的 `VERSION` 加一，否則已安裝離線快取的手機會一直吃舊的 payload
- [ ] 本機 `python -m web.server` 仍能啟動並處理（兩條路並存）
