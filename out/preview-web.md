# 手機版產出預覽報告

自動驗收只能確認這份報告有被產生出來；以下項目請人眼過目，並依 `CHECKLIST-B.md` 用手機實機再跑一次比對。

> 本報告不是在真實瀏覽器裡跑出來的。驗收全程不得連外部服務，無法從 CDN 載入 Pyodide，
> 因此改以 CPython 3.10 執行 `docs/app.js` 內嵌的同一段 Python 膠水程式，
> 在解開的 `docs/payload/pipeline.zip` 目錄中處理 `tests/fixtures/mht/` 的五份 MHT，名冊用打包的快取（與手機版相同）。

## 這次執行

- 整體結果：成功
- 八階段：S1 成功、S2 成功、S3 成功、S4 成功、S5 成功、S6 成功、S7 成功、S8 成功
- 略過的檔案：無
- 日誌事件數：39（info 30、warn 9、error 0）
- 產出報表大小：56,894 bytes（手機上會成為 Blob URL，檔名 `report-<YYYYMMDD-HHMM>.html`）

## 打包內容

- `docs/payload/pipeline.zip`：37,793 bytes，32 個檔案
- 名冊快照抓取日期：2026-09-12（12 隊）

## 交叉驗證（請特別看這裡）

- 對左投：重建 54 打數 9 安打，原站官方 52 打數 8 安打 → 不符
- 對右投：重建 143 打數 38 安打，原站官方 157 打數 41 安打 → 不符

本機窗口與測試用的是 `tests/fixtures/roster/` 的 5 隊 Stub 名冊，對左可重建出官方的 52 打數 8 安打；
手機版改用 `cache/` 的 12 隊真實名冊快照，比對結果不同。這是名冊資料來源的差異，
不是 B 計畫改動造成的退化（同一份 MHT 以 Stub 名冊跑，產出與改動前逐字相同，只差時間戳）。

已查證的差異來源：Stub 名冊沒有 DeNA 與ヤクルト，DeNA 左投「東 克樹」（2 打數）在 Stub 下未命中、不計入；
12 隊快取補上他的左右手後，這 2 打數被算進對左，但原站官方的對左合計不含這筆（稽核表 DeNA ab −2 同源）。
換句話說，黃金值 52 打數 8 安打是在「東 克樹未命中」的前提下成立的。要以哪個為準屬於資料判斷，請你決定。
詳見 DECISIONS.md「B 計畫貴批次的自主決策」與建議後續。

## 左右手未命中（一律留空，不猜）

- 共 0 位

## 一致性稽核差異

| 球團 | 欄位 | 差額 |
|---|---|---|
| ロッテ | ab | 1 |
| ソフトバンク | ab | 16 |
| ソフトバンク | h | 3 |
| ソフトバンク | hr | 1 |
| DeNA | ab | -2 |
| DeNA | h | -1 |
| 阪神 | ab | -2 |
