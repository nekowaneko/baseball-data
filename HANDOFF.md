# 林安可配球對照表 — 工作流交接文件

給 Claude Code 與 Claude Dispatch。這份文件記錄一次人工完成的完整流程，以及要把它自動化時必須知道的坑。

---

## 1. 這條流程在做什麼

把一個日職打者的公開數據，從「彼此不相關的多張表」整理成「可以交叉閱讀的互動網頁」。

核心矛盾是：資料源（Short-Stop）提供**配球**細到「球團 × 左右投 × 球數 × 球種」，但**打擊成績**只到「球團合計」與「左右合計」兩張互不相交的邊際表。兩者無法直接 join，必須從逐投手成績自行重建交叉表，而重建所需的「投手左右手」欄位資料源沒有，得去 NPB 官方名冊補。

---

## 2. 實際執行過的步驟

### S1. 取得原始檔

使用者手動存檔 5 份 MHT，對應 Short-Stop 同一頁的不同分頁。

**為什麼是 MHT 而不是直接抓網址**：short-stop.jp 是 Next.js 前端渲染，`web_fetch` 只拿得到 HTML 骨架與 `<title>`，數據全部是空的。MHT 是瀏覽器存下的「JavaScript 執行後」快照，資料才在裡面。

**這一步無法自動化**，因為它需要真人在瀏覽器裡點開分頁並等待載入。自動化版本只能接收檔案，不能自己去產生檔案。

### S2. 解析 MHT

MHT 是 MIME 多部件容器。用 Python `email` 模組解析，取 `Content-Location` 對應目標頁的 `text/html` 部件，再用 bs4 + lxml 處理。

五份檔案的內容分別是：基本成績、球種情報、狀況別、對戰成績（逐投手）、球團別配球。**檔名不可靠**（都是 `..._(1).mht` 這種），必須依內容特徵判斷是哪一個分頁。

兩個非顯而易見的解析點：

- **配球堆疊圖不是 SVG**，是 div 排出來的。數值藏在每個色塊的 `title="ストレート: 44.2%"` 屬性裡。整份共 675 個色塊。
- **切分「對右投手／對左投手」區塊時不能用字串比對**，因為按鈕的 `title` 屬性也含有「対左投手」字樣，會在錯誤位置切開。必須比對 `>対右投手</h2>` 這種帶標籤的形式。

### S3. 一致性稽核

這一步是後來補上的，但它抓到了兩個真實缺漏，**不可省略**。

做法：把逐投手成績依球團加總，跟球團別成績表逐項比對打數、安打、全壘打。

實際結果：8 隊中 5 隊完全吻合；羅德差 1 個打數；**軟銀差 16 個打數、3 支安打、1 轟**（逐投手表只加到 36，官方是 52）。原因推測是存檔當下該區塊尚未完全載入。頁面上沒有「更多」按鈕，所以從 DOM 判斷不出來，只有靠加總比對才會浮現。

### S4. 補投手左右手

資料源沒有這個欄位。逐投手表裡有 86 位投手的連結，但點進去也是 JS 渲染頁，抓了沒用。

改用 NPB 官方名冊：`https://npb.jp/bis/teams/rst_{code}.html`，伺服器端渲染，表格裡直接有「投」欄（右／左），且含育成選手。球團代碼：`e` 樂天、`f` 日本火腿、`b` 歐力士、`m` 羅德、`h` 軟銀、`l` 西武、`s` 養樂多、`db` DeNA、`t` 阪神、`g` 巨人、`d` 中日、`c` 廣島。

**成本判斷**：86 位投手逐一查是 86 次請求；改抓 8 頁球團名冊再本地比對，是 8 次。實際只抓了 5 頁（5 支太平洋聯盟球隊），因為交流戰的央聯投手總共只涉及 4 個打數，遠低於顯示門檻，抓了也不會顯示。

### S5. 姓名比對

正規化：`unicodedata.normalize('NFKC', name)` 後去除所有空白。處理全半形空格與寫法差異。

主鍵是 `球團|姓名`，查不到時退回 `姓名` 全聯盟查找。**這個回退不是防禦性程式碼，是必需的**：實測有三位投手的球團歸屬在 Short-Stop 是錯的（藤原大翔、大竹風雅被列為阪神但實為軟銀；尾形崇斗列為 DeNA，實為軟銀於 5/13 轉隊）。沒有回退就會有 3 人落入未命中。

**查不到就留空，絕對不猜。** 未命中要列成清單輸出。

### S6. 聚合

依球團 × 左右手加總。

**可精確相加**：打數、安打、全壘打、三振。
**不可相加**：OPS、上壘率。重算需要四壞球與二三壘打，來源表沒有這兩欄。所以微觀層沒有 OPS，這是資料限制不是實作偷懶。

### S7. 驗證（關鍵）

聚合完成後，把所有球團的左投打數加總，跟原站「對左」欄位比對。

實測：**52 打數 8 安打，與原站完全相符**。這代表 82 筆左右手判定全部正確——只要有一位歸錯邊，這個數字就會偏掉。對右側加總是 141/38，與原站 157/41 的差距正好等於 S3 抓到的軟銀缺口，兩個異常互相印證。

**這個驗證要保留在自動化版本裡，它是整條流程唯一的正確性保證。**

### S8. 產出 HTML

單一檔案，資料內嵌，離線可開。分兩層：綜觀（不分球團的左右投表現）與微觀（單一球團下的左右投配球與成績）。

已知缺口固定印在頁尾，不藏在對話裡——使用者日後單獨開檔時不會記得這些前提。

---

## 3. 要自動化的東西

一個本機網頁窗口，接收多個 MHT，顯示處理進度，出錯時顯示可追蹤的 log。

### 階段切分（也就是進度條的格子）

| 階段 | 名稱 | 失敗時的行為 |
|---|---|---|
| S1 | 接收與分類檔案 | 無法分類的檔案標記略過，不中止 |
| S2 | 解析各分頁 | 該分頁標記失敗，其餘繼續 |
| S3 | 一致性稽核 | 永不中止，差異寫進報告 |
| S4 | 取得投手左右手 | 取不到的球團標記未命中，繼續 |
| S5 | 姓名比對 | 未命中列清單，繼續 |
| S6 | 聚合統計 | 缺資料的格子留空，繼續 |
| S7 | 交叉驗證 | 不符時**不中止**，但在報告與網頁上標紅 |
| S8 | 產出 HTML | 失敗才算整體失敗 |

**設計原則：只有 S8 失敗才算整個流程失敗。** 前七階段一律降級處理並記錄，因為資料本來就會缺，中止等於逼使用者反覆重跑。

### 為什麼需要後端

瀏覽器抓不了 npb.jp（CORS）。MHT 解析理論上前端做得到，但既然 S4 需要後端，整條管線放後端比較單純，前端只負責上傳、顯示進度、顯示 log、下載結果。

---

## 4. 已知會再遇到的坑

1. **MHT 存檔不完整**：分頁沒點開或沒載入完就存，資料會缺，而且 DOM 上看不出來。S3 稽核是唯一的偵測手段。
2. **球團歸屬不可信**：以 NPB 名冊為準，S5 的跨隊回退必須保留。
3. **樣本極小**：拆成球團 × 左右後每格只剩 6 到 34 個打數。打數低於 10 時不顯示打率。即使超過門檻也要把打數一起顯示。
4. **NPB 頁面很大**：單頁約 9000 token。務必快取到本地，不要每次重抓。
5. **賽季進行中**：名冊會變動（轉隊、育成升支配下）。快取要帶日期，過期就重抓。

---

## 5. 檔案清單（本次產出）

- `lin-an-ko-2026-pitching.html` — 成品網頁
- `hands_raw.txt` — 投手左右手對照表，82 筆，來源為 NPB 名冊「投」欄
- `build_splits.py` — 比對與聚合腳本
- `vs_pitchers.json` — 逐投手對戰成績
- `splits.json` — 球團 × 左右的聚合結果與未命中清單
- `data.json` — 網頁內嵌的完整資料

這些檔案可直接當作自動化版本的測試 fixture 與期望值。特別是 `splits.json` 的「對左 52 打數 8 安打」，拿來當回歸測試的黃金值。
