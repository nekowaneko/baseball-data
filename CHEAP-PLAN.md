# 配球資料處理窗口 — 便宜批次執行計畫

> 這份文件給協作代理人自主執行。
> 本批次為純產出工作，不含修正迴圈，可用較便宜的模型執行。
> 執行順序：本批次先跑，完成後才執行 EXPENSIVE-PLAN.md。

## 0. 執行協定

- **全程不得向使用者提問。** 未明訂處依 §4 預設值表處理，並記錄在 DECISIONS.md。
- 本批次的驗收都是結構性檢查（檔案存在、章節齊全、格式正確、字數上限）。失敗時直接補齊缺漏即可，**不需要也不應該進入反覆修正的迴圈**。
- 若某項驗收補齊兩次仍不通過，停止該項並寫入 BLOCKERS.md，繼續處理其他項目。
- 每完成一個階段就 commit。開工前檢查有無 .git：沒有則 git init，有則直接使用並先把既有未提交檔案 commit 保存。全程不設遠端、不執行 push。
- 所有註解、文件、commit 訊息使用繁體中文（臺灣慣用用法：品質、資料、程式）。

## 1. 本批次範圍

| 動作 | 對象 |
|---|---|
| 建立 | 目錄骨架、設定範例、常數資料檔、文件 |
| 填入內容 | 球種與球團對照表、階段定義表、README、CHECKLIST |
| 不動 | 任何 `.py` / `.js` 程式邏輯（屬於 EXPENSIVE-PLAN.md） |

## 2. 產出檔案清單（交接契約）

本批次完成後，以下檔案必須存在且非空。EXPENSIVE-PLAN.md 的 T0 會檢查這份清單：

- `README.md` — 專案用途、啟動方式、資料流概述
- `config/stages.json` — 八個處理階段的定義（進度條依此渲染）
- `config/pitch_meta.json` — 球種的固定排序、顏色、中日對照
- `config/team_meta.json` — 球團中日對照與 NPB 名冊代碼
- `config/settings.example.json` — 設定範例檔，不得含真實路徑以外的敏感資訊
- `docs/DATA-SCHEMA.md` — 中介資料格式規格
- `docs/KNOWN-GAPS.md` — 已知資料缺口，內容取自 HANDOFF.md §4
- `CHECKLIST.md` — 交付後由使用者人工掃的清單
- 空目錄骨架：`core/`、`adapters/`、`web/`、`tests/`、`fixtures/`、`cache/`、`out/`（各放一個 `.gitkeep`）

## 3. 各檔案的內容規格

### config/stages.json

固定內容，直接寫入：

```json
{
  "stages": [
    { "id": "S1", "name": "接收與分類檔案", "fatal": false },
    { "id": "S2", "name": "解析各分頁", "fatal": false },
    { "id": "S3", "name": "一致性稽核", "fatal": false },
    { "id": "S4", "name": "取得投手左右手", "fatal": false },
    { "id": "S5", "name": "姓名比對", "fatal": false },
    { "id": "S6", "name": "聚合統計", "fatal": false },
    { "id": "S7", "name": "交叉驗證", "fatal": false },
    { "id": "S8", "name": "產出 HTML", "fatal": true }
  ]
}
```

只有 S8 的 `fatal` 為 true。這是刻意的：前七階段一律降級並記錄，不中止整條管線。

### config/pitch_meta.json

需包含 `order`（固定排序陣列）、`colors`（球種對色碼）、`zh`（中日對照）三個鍵。

固定順序為速球系 → 橫向變化 → 縱向曲球 → 下墜球：

```
ストレート, ツーシーム, ワンシーム, シュート,
スライダー, カットボール, スイーパー, 縦スライダー,
カーブ, ナックルカーブ, スローカーブ,
フォーク, スプリット, チェンジアップ, シンカー
```

顏色與中日對照從既有成品 `lin-an-ko-2026-pitching.html` 的 `PITCH_COLORS` 與 `PITCH_ZH` 兩個常數原樣搬過來，不要自行調色。

### config/team_meta.json

每一隊需有 `zh`（中文名）與 `npb_code`（名冊代碼）。代碼對照：

```
楽天 e、日本ハム f、オリックス b、ロッテ m、ソフトバンク h、
西武 l、ヤクルト s、DeNA db、阪神 t、巨人 g、中日 d、広島 c
```

### docs/DATA-SCHEMA.md

規格文件，上限 800 字。必須包含的章節（順序固定）：

- `vs_pitchers.json` — 逐投手對戰成績的欄位定義
- `pitchmix.json` — 球團 × 左右 × 球數 × 球種的巢狀結構
- `splits.json` — 聚合結果與 `unmatched` 清單
- `run.ndjson` — 執行日誌，每行一個事件，欄位為 `ts`、`stage`、`level`、`msg`、`detail`

欄位定義直接參照既有產出檔，不要自創新欄位。

### docs/KNOWN-GAPS.md

上限 600 字。內容取自 HANDOFF.md §4 的五點，逐點改寫成「症狀 / 偵測方式 / 處理方式」三段式。

### CHECKLIST.md

給使用者人工掃的項目，上限 400 字。至少包含：

- 產出網頁在手機寬度下沒有文字截斷
- 球種顏色在白底上彼此分得開
- 進度條在全部成功、部分失敗、完全失敗三種情況下各看過一次
- log 面板能複製出完整錯誤
- 頁尾的已知缺口有正確反映本次執行的實際狀況

## 4. 預設值表（不要為這些提問）

| 問題 | 預設值 |
|---|---|
| 專案根目錄名稱 | `baseball-data`（即本 repo 根目錄，不要再建一層子資料夾） |
| 設定檔格式 | JSON（不用 YAML，避免多一個相依套件） |
| 文件語言 | 繁體中文，臺灣慣用用法 |
| 顏色與對照表有疑義時 | 以既有成品 HTML 內的常數為準，不自行調整 |
| 字數超出上限時 | 精簡內容，不得刪除必要章節 |
| 需要使用者本人判斷的內容 | 用 `[[待確認：說明]]` 佔位，不自行編造 |

## 5. 驗收清單

全部為結構性檢查，用 grep 或解析完成，不需要執行任何程式邏輯。

| # | 驗收項 | 判定方式 |
|---|---|---|
| C01 | §2 清單中的檔案全部存在且非空 | 逐一檢查 |
| C02 | 三個 config JSON 可被 `json.load` 解析 | 解析 |
| C03 | `stages.json` 恰有 8 個階段，且僅 S8 的 fatal 為 true | 解析並斷言 |
| C04 | `pitch_meta.json` 的 `order` 恰有 15 個球種，且首項為 `ストレート` | 解析並斷言 |
| C05 | `team_meta.json` 涵蓋全部 12 隊，代碼無重複 | 解析並斷言 |
| C06 | `DATA-SCHEMA.md` 四個必要章節齊全，字數 ≤ 800 | 解析標題並計數 |
| C07 | `KNOWN-GAPS.md` ≤ 600 字、`CHECKLIST.md` ≤ 400 字 | 計數 |
| C08 | 專案內無 `[[待確認：` 殘留在應為實質內容的檔案（三個 config） | grep |
| C09 | 目錄骨架七個資料夾皆存在 | 逐一檢查 |
| C10 | git 工作區乾淨 | `git status --porcelain` 為空 |

**不得為了通過驗收而放寬標準、刪減章節、或改寫驗收條件本身。**

## 6. 絕對禁止事項

- 不得向使用者提問。
- 不得撰寫任何 `.py` 或 `.js` 的程式邏輯。
- 不得建立含真實憑證的檔案，只建範例檔。
- 不得刪除或覆蓋既有檔案；有衝突則將既有檔案改名保留。
- 不得設遠端、不得 push。
- 不得執行 EXPENSIVE-PLAN.md 的任何內容，即使看起來很順手。
- 不得抄錄 Short-Stop 或 NPB 的網頁原文段落到文件裡；只寫欄位名稱與結構。

## 7. 完成後的通知格式

輸出以下格式，不要輸出多餘內容：

    ✅ 便宜批次完成，請查核。

    驗收結果：C01–C10 全部通過（或：通過 n 項，m 項未通過，詳見 BLOCKERS.md）
    產出檔案：<n> 個
    Commit 數：<n>
    補齊次數：<n>

    需要你人工確認的部分：
    1. config/pitch_meta.json 的顏色是否與你既有成品一致

    自主決策列在 DECISIONS.md，共 <n> 項。

## 8. 故意不做的事

- 不實作解析、比對、聚合、伺服器、前端任何一段程式——那屬於 EXPENSIVE-PLAN.md。
- 不下載任何網頁、不建立快取內容。
- 不執行測試套件。
- 不自行擴充範圍；有建議寫進 DECISIONS.md 的「建議後續」段落，不要實作。
